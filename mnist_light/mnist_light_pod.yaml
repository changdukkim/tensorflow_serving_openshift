apiVersion: v1
kind: Pod
metadata:
  name: mnist-training
spec:
  restartPolicy: OnFailure
  containers:
    - name: mnist-training
      image: 172.30.1.1:5000/mnist/mnist-training:latest
      imagePullPolicy: Always
